{
  "name": "find-it-faster",
  "displayName": "FindItFaster",
  "publisher": "xxx",
  "repository": {
    "url": "https://github.com/tomrijndorp/vscode-findfast"
  },
  "description": "Find it, but faster! Leveraging fzf and rg.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.56.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:find-it-faster.shellThing",
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "find-it-faster.invoke",
        "title": "Find It Faster: search file"
      },
      {
        "command": "find-it-faster.invokeWithin",
        "title": "Fint It Faster: search within files"
      }
    ],
    "keybindings": [
      {
        "command": "find-it-faster.invoke",
        "key": "cmd+shift+j"
      },
      {
        "command": "find-it-faster.invokeWithin",
        "key": "cmd+shift+u"
      }
    ],
    "configuration": {
      "title": "FindItFaster",
      "properties": {
        "find-it-faster.general.VS Code Path": {
          "markdownDescription": "Unfortunately, we need this for various reasons, and we can't automatically read it.",
          "type": "string",
          "default": "/Applications/Visual Studio Code.app"
        },
        "find-it-faster.general.showPreview": {
          "description": "show a preview when using FZF",
          "type": "boolean",
          "default": true
        },
        "find-it-faster.general.previewCommand": {
          "markdownDescription": "Command that is used by `fzf` to produce the preview. Use `{}` to indicate the filename. Example: `cat {}`. Recommendation: install `bat` and have code highlighting using e.g. `bat --force-colorization --theme='1337' --plain {}`! This option does not do anything if previews are turned off.",
          "type": "string",
          "default": "bat --force-colorization --plain {}"
        },
        "find-it-faster.general.hideTerminalAfterSuccess": {
          "markdownDescription": "Extensions have limited control over the terminal panel. Ideally, it would be possible to automatically switch back to the previous thing you were doing (e.g. hide the terminal, show the terminal you're working on, show the tasks panel if that's what you were on), but we can't. Select here whether you want the terminal to be hidden or remain shown after invoking a command that fails / is canceled.",
          "type": "boolean",
          "default": false
        },
        "find-it-faster.general.hideTerminalAfterFail": {
          "markdownDescription": "Extensions have limited control over the terminal panel. Ideally, it would be possible to automatically switch back to the previous thing you were doing (e.g. hide the terminal, show the terminal you're working on, show the tasks panel if that's what you were on), but we can't. Select here whether you want the terminal to be hidden or remain shown after invoking a command that succeeds.",
          "type": "boolean",
          "default": false
        },
        "find-it-faster.general.clearTerminalAfterUse": {
          "markdownDescription": "Sends a 'clear' command after invoking a terminal command for a cleaner look",
          "type": "boolean",
          "default": false
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "vscode:package": "vsce package"
  },
  "devDependencies": {
    "@types/glob": "^7.1.3",
    "@types/mocha": "^8.2.2",
    "@types/node": "14.x",
    "@types/vscode": "^1.56.0",
    "@typescript-eslint/eslint-plugin": "^4.26.0",
    "@typescript-eslint/parser": "^4.26.0",
    "eslint": "^7.27.0",
    "glob": "^7.1.7",
    "mocha": "^8.4.0",
    "typescript": "^4.3.2",
    "vscode-test": "^1.5.2"
  }
}